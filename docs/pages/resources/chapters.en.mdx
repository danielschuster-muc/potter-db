# Chapters
This resource provides information about chapters of a specific book from the Harry Potter Universe.

## Attributes
{
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
      <th>Filterable</th>
    </tr>
  </thead>
  <tbody>
    {[
      {
        name: "order",
        type: "integer",
        description: "The chronological order of this chapter within the book.",
        filterable: true,
      },
      {
        name: "slug",
        type: "string",
        description: "A slug to identify this chapter.",
        filterable: false,
      },
      {
        name: "summary",
        type: "string",
        description: "The summary of this chapter.",
        filterable: true,
      },
      {
        name: "title",
        type: "string",
        description: "The title of this chapter.",
        filterable: true,
      },
    ].map((item) => (
      <tr key={item.name}>
        <td>{item.name}</td>
        <td>{item.type}</td>
        <td>{item.description}</td>
        <td>{item.filterable == true ? "✅" : "❌"}</td>
      </tr>
    ))}
  </tbody>
</table>
}
