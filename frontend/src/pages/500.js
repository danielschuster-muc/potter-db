import { Typography } from "@mui/material";

const InternalServerError = () => {
  return <Typography variant="h3">500 - Server-side error occurred</Typography>;
};

export default InternalServerError;
