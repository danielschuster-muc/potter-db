import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#aa00ff",
    },
    secondary: {
      main: "#00c853",
    },
  },
});

export default Theme;
