import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#d500f9",
    },
    secondary: {
      main: "#00c853",
    },
  },
});

export default Theme;
